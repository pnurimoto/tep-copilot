{
  "name": "tep-control-structure-replay",
  "version": "0.1.0",
  "private": true,
  "description": "TEP control-structure replay verifier and SVG renderer smoke tests.",
  "scripts": {
    "test": "bash tests/run_all.sh",
    "test:renderer": "bash tests/run_all.sh"
  },
  "devDependencies": {
    "esbuild": "^0.27.0"
  }
}
